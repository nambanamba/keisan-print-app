# 作業メモ（keisan-print-app）

## 2026-09-06

### 1. 出題順をテキストの掲載順に固定（完了）
- `index.html` から `shuffleArray()` を削除し、`pool.slice(0, calcCount)` に変更
- `QA_DATA` は id昇順＝回→ページ→問番号の掲載順なので、ソート不要でそのまま使える
- 設定画面に「テキストの掲載順に、範囲の先頭から出題します」と明示

### 2. 正解の記録＋未正解のみ出題（完了）
ユーザー要望「正解した問題を登録できるようにし、正解していない問題に切り替えられるように」。
**ROLE.mdの「無状態アプリ」という前提を変更したので、ROLE.md・README.mdも更新済み。**

- 保存先：localStorage キー `keisan_print_stats_v1`
- 形：`{ 問題id: {correct, wrong, lastAnswered, lastCorrectAt} }`
  - kosukequiz-battle の `kq_battle_stats_v1` と同じ「idをキーにした表」の形にしてある
  - **box/nextDue（間隔反復）は持たない。** ユーザーが「未正解のみ出題トグル」を選択したため。
    必要になったら同じidに後から足せる
- 記録のつけ方は2通り（ユーザーが両方を選択）
  1. 練習画面：「こたえを見る」→「⭕ できた／❌ できなかった」でその場で記録
  2. 一覧画面：「✅ 正解した問題を登録する」→ 紙で解いた分をチェック（その場で保存）
- 設定画面に「正解した問題を除く（未正解のみ出題）」トグルと、
  「第2回：70問中 12問 正解ずみ（のこり 58問）」の件数表示を追加
- 「🗑️ 正解の記録をすべて消す」でリセット可能
- localStorage が使えない環境（プライベートモード等）でも落ちないよう try/catch 済み

### 3. 親問の文脈表示（要望②への対策）
「未正解のみ」を有効にすると、親問が正解ずみで除外され、子問だけが残って
解けなくなるケースがある（例：r2c69 発展問題(1) が除外され、r2c70「同じ装置で〜」だけ残る）。
対策として、前問依存の言い回し（同じ／前の問／この装置…）を含む問題で、
同じ図を共有する親問が出題対象から外れている場合だけ、
**【前の問題】ここまでの条件** として親問の文と図を添えるようにした（画面・印刷の両方）。
親問も一緒に出題されるときは重複しないよう出さない。

### 4. 既存バグの解消（副次）
範囲・トグルを変えたときに `calcQueue` を捨てるようにしたため、
「一度練習したあと範囲を変えて印刷すると古い問題セットが印刷される」バグも直った。

### 動作確認
Playwright + Chromium で23項目のE2Eテストを実行し、全項目パス・JSエラーなし。
（件数表示／掲載順／記録の中身／未正解のみ／一覧チェック／リセット／文脈表示／印刷HTML）

---

## 担当範囲の整理（司令塔claude-1cからの依頼）

### アプリ側（このリポジトリ）
出題順、親子の並び、正解の記録と絞り込み、印刷レイアウト → 上記のとおり対応済み

### 元データ側（`quiz_csv_理科`）
`images/` の切り出し範囲そのもの、`q`（問題文）の自己完結性

調査でわかったこと：
- `quiz_csv_理科\画像切り出しリスト_第2回.md` に「全40枚、すべて切り出し・配置済み」とあり、
  **PyMuPDF(fitz)で `理科演習.pdf`・`理科.pdf` のページをレンダリングし、座標指定で切り出している**
- 切り出しは「図だけ」で、リード文・条件は含まれない（例：`r2_21.jpg` は左端の「40g」が切れている）
- 条件は画像ではなく `q` にテキストで書き直して入っている
- 元PDFはGoogleドライブにあるが40〜64MBあり、cloudセッションからは取り込めない。
  **再切り出しは作成担当のPCでの作業になる**
- ユーザーの判断：**元データ側（理科の作成担当）に再切り出しを依頼する**

## 次にやること
1. **理科の作成担当への依頼**：リード文・条件・図をひとまとまりにした切り出しへの作り直し
   （テキストの前から順に、(2)だけを切り出さない）。画像が差し替わったら `images/` を更新する
2. 先頭N問しか出せない件：掲載順に固定した結果、「第2回・10問」だと常に先頭10問になる。
   ただし「未正解のみ」を使えば解いた分から進むので、実用上は解消の見込み。しばらく様子見
3. 記録は端末ごと。別端末と共有したくなったら kosukequiz-battle 側との連携を検討

## データの現況
全77問（第1回7問、第2回70問）、画像39枚、`img`無しは4問（r1c01, r1c02, r2c51, r2c52）
