# 作業メモ（keisan-print-app）

## 2026-09-06

### やったこと：出題順をテキストの掲載順に固定
ユーザー要望「出題順はランダムにせず、テキストの前からそのままの順序に」を実装。

- `index.html` から `shuffleArray()` を削除
- `currentCalcQueue()` を `pool.slice(0, calcCount)` に変更（掲載順のまま先頭から切り出す）
- 設定画面に「テキストの掲載順に、範囲の先頭から出題します（ランダムではありません）」と明示
- `QA_DATA` は id昇順＝回→ページ→問番号の掲載順に並んでいるので、
  ソートは不要でそのまま使える（77問すべてで順序の逆転が無いことを確認済み）

**副次効果**：親問（問5-(1)）が子問（問5-(2)）より必ず先に印刷されるようになった。
「同じ装置で」等、前問に依存する文面の問題が19/77件あるため、これは重要。

### 担当範囲の整理（司令塔claude-1cからの依頼）
- **アプリ側（このリポジトリ）**：出題順・親子の並び・印刷レイアウト
- **元データ側（`quiz_csv_理科` → kosukequiz-battle経由）**：`images/`の切り取り範囲そのもの、`q`文の自己完結性
  - 画像は「図だけ」のトリミングで、リード文・条件は含まれていない（例：`r2_21.jpg` は左端の「40g」が切れている）
  - 要望1「テキストの前から素直に画像で切り取る」・要望2「(2)だけを切り出さない」の
    **画像トリミングの部分は元データ側の領分**。このアプリでは対応できない

### 次にやること（ユーザー判断待ち）
1. **先頭N問しか出せない問題**：第2回は70問あるが「10問」指定だと常に先頭10問。
   「何問目から」の開始位置指定 or 「続きから」ボタンが要るか要確認
2. **印刷ボタンのキュー使い回しバグ**（既存）：一度練習すると `calcQueue` が残り、
   設定画面で範囲を変えて印刷しても古いキューが印刷される（`index.html` の
   `print-problems-btn` / `print-answers-btn` の `calcQueue.length ? calcQueue : ...`）。
   要望と直接関係ないので今回は手を付けていない
3. 同じ図を共有する連続問題を印刷時に1ブロックにまとめる案（図の重複を減らす）

### データの現況
- 全77問（第1回7問、第2回70問）、画像39枚、`img`無しは4問（r1c01, r1c02, r2c51, r2c52）
